name: _Reusable Build Containers (internal use only)

on:
  workflow_call:
    inputs:
      container_type:
        description: 'Type of container to build'
        required: true
        type: string  # 'core', 'full', 'dev', 'custom', 'experiment'

      # Common inputs
      build_platforms:
        description: 'Platforms to build for'
        required: false
        default: 'linux/amd64,linux/arm64'
        type: string

      image_prefix:
        description: 'Image prefix (e.g., "sst", "sst-dev")'
        required: true
        type: string

      tag_suffix:
        description: 'Tag suffix for the built images'
        required: true
        type: string

      # SST-specific inputs
      sst_version:
        description: 'SST version (for release builds)'
        required: false
        type: string

      sst_elements_version:
        description: 'SST-elements version override (defaults to sst_version)'
        required: false
        type: string

      mpich_version:
        description: 'MPICH version'
        required: false
        default: '4.0.2'
        type: string

      # Custom/Experiment specific
      sst_core_repo:
        description: 'SST-core repository URL'
        required: false
        type: string

      sst_core_ref:
        description: 'SST-core reference'
        required: false
        type: string

      sst_elements_repo:
        description: 'SST-elements repository URL'
        required: false
        type: string

      sst_elements_ref:
        description: 'SST-elements reference'
        required: false
        type: string

      experiment_name:
        description: 'Experiment name (for experiment builds)'
        required: false
        type: string

      base_image:
        description: 'Base image for experiment builds'
        required: false
        type: string

      # Build configuration
      containerfile_path:
        description: 'Path to Containerfile'
        required: false
        default: 'Containerfiles/Containerfile'
        type: string

      docker_context:
        description: 'Docker build context'
        required: false
        default: 'Containerfiles'
        type: string

      build_target:
        description: 'Docker build target'
        required: false
        type: string

      # Advanced options
      force_rebuild:
        description: 'Force rebuild even if images exist'
        required: false
        default: false
        type: boolean

      ignore_cache:
        description: 'Ignore build cache'
        required: false
        default: false
        type: boolean

      enable_perf_tracking:
        description: 'Enable SST performance tracking'
        required: false
        default: false
        type: boolean

    outputs:
      built_images:
        description: 'JSON array of built image tags'
        value: ${{ jobs.create-manifest.outputs.built_images }}

      manifest_tag:
        description: 'Fat manifest tag'
        value: ${{ jobs.create-manifest.outputs.manifest_tag }}

      build_successful:
        description: 'Whether build was successful'
        value: ${{ jobs.create-manifest.outputs.manifest_tag != '' }}

env:
  REGISTRY: ghcr.io
  BUILD_NCPUS: 4

jobs:
  prepare-platforms:
    runs-on: ubuntu-latest
    outputs:
      platforms: ${{ steps.setup.outputs.platforms }}
    steps:
      - name: Setup platforms
        id: setup
        run: |
          PLATFORMS="${{ inputs.build_platforms }}"
          echo "platforms=$(echo "$PLATFORMS" | jq -R -c 'split(",")' )" >> $GITHUB_OUTPUT

  prepare-image-config:
    runs-on: ubuntu-latest
    outputs:
      image_prefix: ${{ steps.config.outputs.image_prefix }}
      core_full_pattern: ${{ steps.config.outputs.core_full_pattern }}
      dev_custom_pattern: ${{ steps.config.outputs.dev_custom_pattern }}
      experiment_pattern: ${{ steps.config.outputs.experiment_pattern }}
      default_pattern: ${{ steps.config.outputs.default_pattern }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure image naming
        id: config
        env:
          CONTAINER_TYPE: ${{ inputs.container_type }}
          IMAGE_PREFIX: ${{ inputs.image_prefix }}
          TAG_SUFFIX: ${{ inputs.tag_suffix }}
          REGISTRY: ${{ env.REGISTRY }}
          ENABLE_PERF_TRACKING: ${{ inputs.enable_perf_tracking }}
          EXPERIMENT_NAME: ${{ inputs.experiment_name }}
        run: ./scripts/orchestration/prepare-image-config.sh

  build-containers:
    runs-on: ${{ matrix.platform == 'linux/arm64' && 'ubuntu-24.04-arm' || 'ubuntu-latest' }}
    needs: [prepare-platforms, prepare-image-config]
    permissions:
      contents: read
      packages: write
    strategy:
      matrix:
        platform: ${{ fromJson(needs.prepare-platforms.outputs.platforms) }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Cache MPICH tarball
        id: cache-mpich
        if: inputs.mpich_version != ''
        uses: actions/cache@v3
        with:
          path: Containerfiles/mpich-${{ inputs.mpich_version }}.tar.gz
          key: mpich-${{ inputs.mpich_version }}

      - name: Download MPICH source
        if: inputs.mpich_version != '' && steps.cache-mpich.outputs.cache-hit != 'true'
        working-directory: Containerfiles
        run: ../scripts/build/download_tarballs.sh --component mpich ${{ inputs.mpich_version }}

      - name: Determine SST cache key and paths
        id: sst-cache-config
        if: inputs.sst_version != ''
        run: |
          # Determine if we need elements based on build target or elements repo input
          if [[ "${{ inputs.build_target }}" == "sst-full" ]] || [[ "${{ inputs.container_type }}" == "full" ]] || [[ -n "${{ inputs.sst_elements_repo }}" ]]; then
            ELEMENTS_VERSION="${{ inputs.sst_elements_version }}"
            if [[ -z "$ELEMENTS_VERSION" ]]; then
              ELEMENTS_VERSION="${{ inputs.sst_version }}"
            fi
            echo "needs_elements=true" >> $GITHUB_OUTPUT
            echo "effective_elements_version=${ELEMENTS_VERSION}" >> $GITHUB_OUTPUT
            echo "cache_key=sst-sources-full-${{ inputs.sst_version }}-${ELEMENTS_VERSION}" >> $GITHUB_OUTPUT
          else
            echo "needs_elements=false" >> $GITHUB_OUTPUT
            echo "cache_key=sst-sources-core-${{ inputs.sst_version }}" >> $GITHUB_OUTPUT
          fi

      - name: Cache SST sources (core-only)
        id: cache-sst-core
        if: inputs.sst_version != '' && steps.sst-cache-config.outputs.needs_elements == 'false'
        uses: actions/cache@v3
        with:
          path: Containerfiles/sstcore-${{ inputs.sst_version }}.tar.gz
          key: ${{ steps.sst-cache-config.outputs.cache_key }}

      - name: Cache SST sources (full)
        id: cache-sst-full
        if: inputs.sst_version != '' && steps.sst-cache-config.outputs.needs_elements == 'true'
        uses: actions/cache@v3
        with:
          path: |
            Containerfiles/sstcore-${{ inputs.sst_version }}.tar.gz
            Containerfiles/sstelements-${{ steps.sst-cache-config.outputs.effective_elements_version }}.tar.gz
          key: ${{ steps.sst-cache-config.outputs.cache_key }}

      - name: Download SST sources
        if: inputs.sst_version != '' && ((steps.sst-cache-config.outputs.needs_elements == 'false' && steps.cache-sst-core.outputs.cache-hit != 'true') || (steps.sst-cache-config.outputs.needs_elements == 'true' && steps.cache-sst-full.outputs.cache-hit != 'true'))
        working-directory: Containerfiles
        run: |
          COMPONENT="sst-core"
          if [[ "${{ steps.sst-cache-config.outputs.needs_elements }}" == "true" ]]; then
            echo "Downloading SST-elements sources..."
            wget https://github.com/sstsimulator/sst-elements/releases/download/v${{ steps.sst-cache-config.outputs.effective_elements_version }}_Final/sstelements-${{ steps.sst-cache-config.outputs.effective_elements_version }}.tar.gz --no-check-certificate
          else
            echo "Skipping SST-elements download for core-only build"
          fi

      - name: Validate SST sources
        if: inputs.sst_version != ''
        working-directory: Containerfiles
        run: |
          # Check core sources always exist
          if [ ! -f "sstcore-${{ inputs.sst_version }}.tar.gz" ]; then
            echo "ERROR: SST-core source file sstcore-${{ inputs.sst_version }}.tar.gz not found"
            exit 1
          fi

          # Check elements sources exist when needed
          if [[ "${{ steps.sst-cache-config.outputs.needs_elements }}" == "true" ]]; then
            if [ ! -f "sstelements-${{ steps.sst-cache-config.outputs.effective_elements_version }}.tar.gz" ]; then
              echo "ERROR: SST-elements source file sstelements-${{ steps.sst-cache-config.outputs.effective_elements_version }}.tar.gz not found but required for build target '${{ inputs.build_target }}'"
              exit 1
            fi
            COMPONENT="sst-full"
          fi
          ../scripts/build/download_tarballs.sh --component "$COMPONENT" ${{ inputs.sst_version }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract platform info
        id: platform
        run: |
          PLATFORM="${{ matrix.platform }}"
          ARCH="${PLATFORM##*/}"
          echo "arch=$ARCH" >> $GITHUB_OUTPUT
          echo "platform_safe=${PLATFORM//\//-}" >> $GITHUB_OUTPUT

      - name: Prepare container tags
        id: tags
        run: |
          ARCH="${{ steps.platform.outputs.arch }}"

          # Use pre-computed patterns based on container type
          case "${{ inputs.container_type }}" in
            "core"|"full")
              TEMP_TAG="${{ needs.prepare-image-config.outputs.core_full_pattern }}-${ARCH}"
              ;;
            "dev"|"custom")
              TEMP_TAG="${{ needs.prepare-image-config.outputs.dev_custom_pattern }}-${ARCH}"
              ;;
            "experiment")
              TEMP_TAG="${{ needs.prepare-image-config.outputs.experiment_pattern }}-${ARCH}"
              ;;
            *)
              TEMP_TAG="${{ needs.prepare-image-config.outputs.default_pattern }}-${ARCH}"
              ;;
          esac

          echo "temp_tag=${TEMP_TAG}" >> $GITHUB_OUTPUT
          echo "Building: ${TEMP_TAG}"

      - name: Build and push container
        uses: docker/build-push-action@v5
        with:
          context: ${{ inputs.docker_context }}
          file: ${{ inputs.containerfile_path }}
          target: ${{ inputs.build_target }}
          platforms: ${{ matrix.platform }}
          build-args: |
            ${{ (inputs.container_type == 'core' || inputs.container_type == 'full') && inputs.sst_version && format('SSTver={0}', inputs.sst_version) || '' }}
            ${{ inputs.sst_elements_version && format('SST_ELEMENTS_VERSION={0}', inputs.sst_elements_version) || '' }}
            ${{ inputs.mpich_version && format('mpich={0}', inputs.mpich_version) || '' }}
            ${{ inputs.sst_core_repo && format('SSTrepo={0}', inputs.sst_core_repo) || '' }}
            ${{ inputs.sst_core_ref && format('tag={0}', inputs.sst_core_ref) || '' }}
            ${{ inputs.sst_elements_repo && format('SSTElementsRepo={0}', inputs.sst_elements_repo) || '' }}
            ${{ inputs.sst_elements_ref && format('elementsTag={0}', inputs.sst_elements_ref) || '' }}
            ${{ inputs.base_image && format('BASE_IMAGE={0}', inputs.base_image) || '' }}
            ${{ inputs.enable_perf_tracking && 'ENABLE_PERF_TRACKING=1' || '' }}
            NCPUS=${{ env.BUILD_NCPUS }}
          labels: |
            com.github.sha=${{ github.sha }}
            com.github.workflow=${{ github.workflow }}
            com.github.run_id=${{ github.run_id }}
            com.github.run_number=${{ github.run_number }}
            com.github.repository=${{ github.repository }}
            com.github.ref_name=${{ github.ref_name }}
            com.container.type=${{ inputs.container_type }}
            sst.perf_tracking=${{ inputs.enable_perf_tracking }}
          tags: ${{ steps.tags.outputs.temp_tag }}
          push: true
          cache-from: type=gha,scope=${{ inputs.container_type }}-${{ inputs.tag_suffix }}-${{ steps.platform.outputs.arch }}
          cache-to: type=gha,mode=max,scope=${{ inputs.container_type }}-${{ inputs.tag_suffix }}-${{ steps.platform.outputs.arch }}

      - name: Output built image
        run: |
          echo "Built image: ${{ steps.tags.outputs.temp_tag }}"

      - name: Check if MPICH artifact exists
        id: check-mpich-artifact
        if: inputs.mpich_version != '' && steps.cache-mpich.outputs.cache-hit != 'true'
        run: |
          ARTIFACT_NAME="mpich-${{ inputs.mpich_version }}"
          echo "artifact_name=${ARTIFACT_NAME}" >> $GITHUB_OUTPUT

          # Check if artifact already exists from another job
          if gh api repos/${{ github.repository }}/actions/runs/${{ github.run_id }}/artifacts --paginate | jq -r '.artifacts[].name' | grep -Fxq "${ARTIFACT_NAME}"; then
            echo "artifact_exists=true" >> $GITHUB_OUTPUT
            echo "MPICH artifact ${ARTIFACT_NAME} already exists, skipping upload"
          else
            echo "artifact_exists=false" >> $GITHUB_OUTPUT
            echo "MPICH artifact ${ARTIFACT_NAME} does not exist, will upload"
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload MPICH artifact (for caching across jobs)
        uses: actions/upload-artifact@v4
        if: inputs.mpich_version != '' && steps.cache-mpich.outputs.cache-hit != 'true' && steps.check-mpich-artifact.outputs.artifact_exists != 'true'
        with:
          name: ${{ steps.check-mpich-artifact.outputs.artifact_name }}
          path: Containerfiles/mpich-${{ inputs.mpich_version }}.tar.gz
          retention-days: 10

      - name: Check if SST sources artifact exists
        id: check-sst-artifact
        if: inputs.sst_version != '' && ((steps.sst-cache-config.outputs.needs_elements == 'false' && steps.cache-sst-core.outputs.cache-hit != 'true') || (steps.sst-cache-config.outputs.needs_elements == 'true' && steps.cache-sst-full.outputs.cache-hit != 'true'))
        run: |
          # Use build-type specific artifact name
          if [[ "${{ steps.sst-cache-config.outputs.needs_elements }}" == "true" ]]; then
            ARTIFACT_NAME="sst-sources-full-${{ inputs.sst_version }}-${{ steps.sst-cache-config.outputs.effective_elements_version }}"
          else
            ARTIFACT_NAME="sst-sources-core-${{ inputs.sst_version }}"
          fi
          echo "artifact_name=${ARTIFACT_NAME}" >> $GITHUB_OUTPUT

          # Check if artifact already exists from another job
          if gh api repos/${{ github.repository }}/actions/runs/${{ github.run_id }}/artifacts --paginate | jq -r '.artifacts[].name' | grep -Fxq "${ARTIFACT_NAME}"; then
            echo "artifact_exists=true" >> $GITHUB_OUTPUT
            echo "SST sources artifact ${ARTIFACT_NAME} already exists, skipping upload"
          else
            echo "artifact_exists=false" >> $GITHUB_OUTPUT
            echo "SST sources artifact ${ARTIFACT_NAME} does not exist, will upload"
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload SST sources artifact (core-only)
        uses: actions/upload-artifact@v4
        if: inputs.sst_version != '' && steps.cache-sst-core.outputs.cache-hit != 'true' && steps.check-sst-artifact.outputs.artifact_exists != 'true' && steps.sst-cache-config.outputs.needs_elements == 'false'
        with:
          name: ${{ steps.check-sst-artifact.outputs.artifact_name }}
          path: Containerfiles/sstcore-${{ inputs.sst_version }}.tar.gz
          retention-days: 10

      - name: Upload SST sources artifact (full)
        uses: actions/upload-artifact@v4
        if: inputs.sst_version != '' && steps.cache-sst-full.outputs.cache-hit != 'true' && steps.check-sst-artifact.outputs.artifact_exists != 'true' && steps.sst-cache-config.outputs.needs_elements == 'true'
        with:
          name: ${{ steps.check-sst-artifact.outputs.artifact_name }}
          path: |
            Containerfiles/sstcore-${{ inputs.sst_version }}.tar.gz
            Containerfiles/sstelements-${{ steps.sst-cache-config.outputs.effective_elements_version }}.tar.gz
          retention-days: 10

  create-manifest:
    runs-on: ubuntu-latest
    needs: [build-containers, prepare-image-config]
    if: needs.build-containers.result == 'success'
    permissions:
      contents: read
      packages: write
    outputs:
      built_images: ${{ steps.manifest.outputs.built_images }}
      manifest_tag: ${{ steps.manifest.outputs.manifest_tag }}
    steps:
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Create multi-architecture manifest
        id: manifest
        run: |
          PLATFORMS="${{ inputs.build_platforms }}"

          case "${{ inputs.container_type }}" in
            "core"|"full")
              MANIFEST_TAG="${{ needs.prepare-image-config.outputs.core_full_pattern }}"
              ;;
            "dev"|"custom")
              MANIFEST_TAG="${{ needs.prepare-image-config.outputs.dev_custom_pattern }}"
              ;;
            "experiment")
              MANIFEST_TAG="${{ needs.prepare-image-config.outputs.experiment_pattern }}"
              ;;
            *)
              MANIFEST_TAG="${{ needs.prepare-image-config.outputs.default_pattern }}"
              ;;
          esac

          echo "Creating manifest: ${MANIFEST_TAG}"

          IFS=',' read -ra PLATFORM_ARRAY <<< "$PLATFORMS"
          MANIFESTS=""
          IMAGES="[]"

          for platform in "${PLATFORM_ARRAY[@]}"; do
            ARCH="${platform#linux/}"
            TEMP_TAG="${MANIFEST_TAG}-${ARCH}"
            IMAGES=$(echo "$IMAGES" | jq -c --arg tag "$TEMP_TAG" '. += [$tag]')

            if docker manifest inspect "${TEMP_TAG}" >/dev/null 2>&1; then
              MANIFESTS="$MANIFESTS ${TEMP_TAG}"
            fi
          done

          echo "built_images=${IMAGES}" >> $GITHUB_OUTPUT

          if [ -n "$MANIFESTS" ]; then
            echo "Creating multi-arch manifest: ${MANIFEST_TAG}"
            docker buildx imagetools create --tag "${MANIFEST_TAG}" $MANIFESTS
            echo "manifest_tag=${MANIFEST_TAG}" >> $GITHUB_OUTPUT
          else
            echo "ERROR: No images found for manifest creation"
            exit 1
          fi
